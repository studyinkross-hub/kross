import {env} from 'cloudflare:workers';
import {isTeacher} from '@/lib/teacher-auth';
const allowed=new Set(['audio/mpeg','audio/mp4','audio/wav','audio/webm','video/mp4','video/webm']);
const media=()=> (env as unknown as {MEDIA:R2Bucket}).MEDIA;
export async function POST(req:Request){if(req.headers.get('origin')&&req.headers.get('origin')!==new URL(req.url).origin)return Response.json({error:'ORIGIN'},{status:403});if(!await isTeacher(req))return Response.json({error:'TEACHER_REQUIRED'},{status:403});try{const form=await req.formData(),file=form.get('file');if(!(file instanceof File)||!allowed.has(file.type)||file.size<1||file.size>250*1024*1024)return Response.json({error:'INVALID_FILE'},{status:400});const ext=file.name.split('.').pop()?.replace(/[^a-z0-9]/gi,'').slice(0,6)||'bin',key=`media/${crypto.randomUUID()}.${ext}`;await media().put(key,file.stream(),{httpMetadata:{contentType:file.type},customMetadata:{name:file.name.slice(0,180)}});return Response.json({url:'/api/media?id='+encodeURIComponent(key),name:file.name,type:file.type,size:file.size});}catch{return Response.json({error:'UPLOAD_FAILED'},{status:500});}}
export async function GET(req:Request){try{const key=new URL(req.url).searchParams.get('id')||'';if(!/^media\/[a-f0-9-]{36}\.[a-z0-9]{1,6}$/i.test(key))return new Response('Not found',{status:404});const object=await media().get(key);if(!object)return new Response('Not found',{status:404});return new Response(object.body,{headers:{'Content-Type':object.httpMetadata?.contentType||'application/octet-stream','Cache-Control':'private, max-age=3600','Accept-Ranges':'bytes'}});}catch{return new Response('Unavailable',{status:503});}
}
